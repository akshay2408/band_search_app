import React from "react";
import ReactDOM from "react-dom";
import Bands from "../components/Bands";

document.addEventListener("DOMContentLoaded", () => {
  ReactDOM.render(<Bands />, document.getElementById("root"));
});
